# Metro cannot follow pnpm's symlinked store reliably, so the mobile app needs
# a hoisted node_modules layout. This file only takes effect when pnpm is run
# from inside apps/mobile; for a root-level `pnpm install` the same line has to
# exist in the repository-root .npmrc (see docs/MOBILE_SETUP.md).
node-linker=hoisted
